# Ambientes Python (recriar com: python -m venv .venv && pip install -r requirements)
.venv/
.venv2/
python/
__pycache__/
*.pyc
.pytest_cache/
# precificador/precificador.db  # desativado: DB com classificacoes versionado para sync automatico

# Artefatos de debug/inspecao (nao usados pelo pipeline)
outputs/**/*.inspect.ndjson
outputs/**/preview.png

# Diretorios de trabalho/temporarios
tmp_ean_extract/
tmp_video/
work/

# Videos de marketing (nao fazem parte do sistema de precificacao)
*.mp4

# Rodadas exportadas: regeneraveis via rodada.py + exportar_excel.py,
# nao precisam ficar versionadas (ficam so localmente).
# PRECIFICACAO_RODADA_*.xlsx  # desativado: RODADA_10 com classificacoes corretas versionada

# Planilha do painel farmaceutico (Brick): 40 mil EANs com VUM, curva, ranking
# e faturamento por produto. O remoto deste repo e' PUBLICO -- o arquivo fica
# so' na maquina, e o atualizar_brick.py o le de outputs/consolidado_estoque/.
outputs/consolidado_estoque/BRICK*.xlsx

# ---- Dado de negocio NUNCA vai para este repo: o origin e PUBLICO ----
# Custo, margem, preco praticado e coleta de concorrente. O que precisa
# viajar entre os 3 PCs sai por exportar_para_sync.py para o repo PRIVADO
# do app (ConsultaPrecosEAN/precificacao/dados), e o ingest le de la.
# O .db nao sincroniza: e binario de 53 MB e cada PC o reconstroi com ingest.py.
precificador/*.db
POLITICA_MARKUP_POR_CATEGORIA.csv
subcategoria_classificada.csv
ESTOQUE_DROGARIA_PRECIFICADO*.xlsx
PRECIFICACAO_RODADA_*.xlsx
decisao_humana_rodada_*.xlsx
eans_nunca_pesquisados.xlsx
fila_coleta*.csv
dados/comparativo_mercado.xlsx
dados/pesquisa_web_*.csv
outputs/**/*.xlsx
outputs/**/*.csv
outputs/**/*.txt
