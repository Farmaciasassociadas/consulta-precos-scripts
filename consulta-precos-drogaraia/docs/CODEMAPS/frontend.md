<!-- Generated: 2026-08-03 | Files scanned: 3 | Token estimate: ~200 -->
# Protótipo web — `web/`

`index.html` (203 linhas) + `app.js` (342 linhas) + `styles.css`. Protótipo
de visualização, ainda não é o app de produção (esse é o desktop Tkinter
em `ConsultaPrecosEAN`).

## `app.js` — funções
| Linha | Função |
|---:|---|
| 103 | `money(v)` — formata BRL |
| 105 | `priceCell(entry)` |
| 113 | `rowStatusClasses(p)` |
| 125 | `dominantStatus(p)` |
| 136 | `renderRows()` — render principal da tabela |
| 173 | `updateChipCounts()` |
| 194 | `applyTheme(mode)` — dark/light |
| 226 | `setRunState(label, dotClass)` |
| 273 | `applyFilters()` |
| 303 | `cellSortValue(tr, key)` |
